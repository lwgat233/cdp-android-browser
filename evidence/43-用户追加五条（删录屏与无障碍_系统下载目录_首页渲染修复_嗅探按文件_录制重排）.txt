用户 2026-09-21 追加的一批（口述）→ 逐条做完并验过
=====================================================
本轮构件：见 MANIFEST 与 apk/测试版/ 同名 .txt（哈希以那份为准）

N1 删掉屏幕录制 + 无障碍残留 ……………………………………… 验收 9/9
  · 界面：遍历所有栏目扫按钮文字，已无「录屏 / 屏幕录制 / 无障碍 / 授权 / 权限」字样
  · 接口：/api/screen、/api/screen/start|stop 都返回"未知接口"；桥的 sys.accessibility 这个 op 删了
  · 权限：实测权限清单里没有 BIND_ACCESSIBILITY_SERVICE，也没有 FOREGROUND_SERVICE_MEDIA_PROJECTION
  · 文件：ScreenRec.kt / ScreenService.kt / CdpDotService.kt / res/xml/cdp_dots.xml 全删；
          AndroidManifest 里的 service 节点与两个权限也清干净
  · 小点保留，但**只画在页面里**（原来那套要无障碍权限盖在视频上，现在不要了）；
    新增 DotBus.kt 只存状态（count/snapshot/isReady=false）
  · 过程中修掉两处自己造成的编译错：Bridge 里 reply 套 reply；DotBus 随服务一起被删

N2 嗅探按文件类型分（不再按视频/音频） ………………………… 验收 5/5
  · 原生 Sniffer：分组＝扩展名本身；排序另按大类（播放列表→视频→音频→分片→字幕）；
    分片合并的判定从 group=="frag" 改成 kind=="ts"||"m4s"；统计改成 {total, byKind:{m3u8:1, ts:1}}
  · 界面：行上的类型标签 = 扩展名（实测 [".m3u8",".ts ×3"]）；状态行 共 2 条：.m3u8 1 条 · .ts 1 条；
    筛选项跟着实际类型走（实测 [all, m3u8, ts]，原来写死的"播放列表/视频/音频/分片/字幕"全删）
  · 「转存 m4a / mp4」的判定改成按 kind（AUDIO_KINDS）
  · 真坑：嗅探**故意跳过 127.0.0.1 的本机请求**（防自嗅探），所以验收页必须走 10.0.2.2，
    否则永远 0 条 —— 这条写进 skill 免得下次又白忙

N3 下载下到系统目录 + 清空旧的 ………………………………… 验收 8/8
  · 下好的文件通过 MediaStore 发布到系统「下载/cdp」（分区存储下的正确做法）
    实测：/sdcard/Download/cdp/sysdir-test.html 真的存在；下载记录里记的是 Download/cdp/... 这个系统路径
  · 应用内副本默认不留（发布成功即删；想留可开设置 keepLocalCopy）
  · 升级后自动清空一次旧的 files/downloads（实测 0 个）
  · 嗅探那栏给人看的"下载目录"字段也改成系统目录（应用暂存目录另给 localDir 字段，不糊弄人）

N4 首页小 app 添加后不渲染（用户报的 bug） …………………… 验收 8/8
  · 根因：新标签页默认地址用的是**内置资源那个 https 源**，混合内容策略把 fetch(本机控制口) 拦掉
    → 页面在、控制台不报错、列表永远空（"有页面但添加不渲染"完全对得上）
  · 修：首页地址统一走「控制台在听就用本机控制口那份」（⌂ / 新标签页 / 新窗口三处）；
    从内置资源入口打开时不再静默空白，给一句"点 ⌂ 打开首页"
  · 实测：控制口入口 5 个格子；内置资源入口 1 个格子（＋）＋明确提示；添加后立刻可见（6 = 5+1）

N5 录制板块重排（列表简洁、动作合并） ………………………… 验收 5/5
  · 顺序改成：步骤框在最上面 → 「载入到步骤列表」挪到**步骤框下面**
  · 录制脚本列表 + 回放**合成一块**（不再散两块）：选脚本 / 次数 / 回放这条 / ▶ 跑当前队列 / 刷新，
    列表下面单独一行「清空录制脚本」，输出框跟着这一块
  · 实测 DOM：步骤框在录制脚本列表之前；载入键在步骤框之后；回放键所在块 data-board=rec-list

还没做完（诚实记账）
  · 列表行规范 2 行制改造：审计现状 **34/34 行不合规**（现有一行制 34px + 全字段 ellipsis 截断），
    规范文件 docs/界面规范-列表行.md 已写、审计脚本 tools/verify_list_rows.py 已能跑；改齐是下一件事
  · 用户点名的大项：重构瘦身 / 全量再验（含测试环境）/ 功能文档补齐 / 逐条"能不能加功能"的结论
  · log.md 28 条里还剩 #22 AI 面板 markdown+豆包式、#28 屏录制改插件形态（现在整块已删，按 N1 作废）、
    #3 广告拦截录制选择器→隐藏规则、#5 嗅探资源可分享、#9 报文按 header 展开+二进制预览、#18 离线翻译
