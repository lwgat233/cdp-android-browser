用户 2026-09-21 追加第三批：按键丑（有颜色）/ 下载后面要有 ✕ / ✕ 弹勾选框小窗 / 录制键也换
=========================================================================

B-80 按键一律黑白（用户原话：「我说您能不能使用简单的没有颜色的按钮（黑白色）啊」「以后配色就不要选有颜色的按键」）
  · style.css 末尾加**灰阶统一块**：普通键＝深灰底(#1E1E1E)浅灰字；主键＝浅灰底(#E4E4E4)深字；
    "危险"不再用红色表达，靠文字直说做什么；栏目键（含抽屉 .tabBtn/.collHead）、mini2 小键、关闭键、
    多窗口浮窗的 ＋、进度条填充、类型标签全部收成灰阶。
  · 顺手把"橙色"的真凶找出来：**⏸(U+23F8) / ▶(U+25B6) 这类字符在 Android 上会被当彩色 emoji 渲染** —— 不是 css 的锅。
    按用户要求改成**文本字形**：暂停＝‖ (U+2016)、继续＝▸ (U+25B8)。
  · 验收 tools/verify_buttons_mono.py：把每个栏目里**可见**按键的计算样式读回来，要求 R≈G≈B（色差 ≤6）。
    实测 **328 个按键、0 个不合规**（抽样：底 rgb(30,30,30)/字 rgb(228,228,228)）。

B-82 下载行三个键（用户原话：「下载的话就显示两竖杠，停止就三角形，✕ 一直在后面。
     下完了就只有 ✕ 在后面，然后点击了 ✕ 就弹出小窗，信息是是否删除本地，有一个 checkbox，下面就是是和否」）
  · 行上：状态键（‖ 正在下 / ▸ 已暂停，点一下切换）+ ✕（一直在行尾）；完成的行只剩 ✕。
  · ✕ → 小窗：标题「删除「名字」？」+ 勾选框「是否删除本地文件」+ 下面「是 / 否」两个键。
  · 真坑一：通用列表渲染器（list.js）会把行内按键**搬进小窗** → ✕ 和状态键都被搬走了（用户看到"后面没有 x"）。
    开白名单：带 `data-row-keep` / `data-state-key` 的键留在行上。
  · 真坑二：N3 之后成品是**发布到系统「下载/cdp」**的那份，`downloads.delete` 只删应用内副本 ＝ "删了但文件还在"。
    改成同时删 MediaStore 里那条（DISPLAY_NAME + RELATIVE_PATH）。
  · 验收 tools/verify_dl_delete_sheet.py **14/14**：不勾→文件留着；勾上→连系统目录里的文件一起删（实测 1→0）；
    点「否」→ 记录和文件都在。
  · 小窗模块（modal.js）补了两样：字段类型 `check`（真勾选框）、`okLabel/cancelLabel`（键就叫「是 / 否」）。

同轮回归（都在这份构件上跑过）
  · 下载暂停/继续 tools/verify_dl_pause.py **18/18**
  · 列表行合规 tools/verify_list_rows.py **46/46 行**
  · 全栏目冒烟 tools/verify_tabs_smoke.py **23/23 栏目干净**
