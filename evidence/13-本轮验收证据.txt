本轮（伪终端 + 屏蔽不渲染）验收证据
时间：2026-09-18 23:34:45 CST

一、构件
  apk/cdp-debug.apk  1703208 字节
  sha256 0f9920da5fdbbb159437d5439a5ac1eaa42ba4ffedda9adaf477deb9ca3b7fe8

二、分组结果
  第一～六组：小结：通过 10 项 / 失败 0 项 / 小结：通过 15 项 / 失败 0 项 / 小结：通过 11 项 / 失败 0 项 / 小结：通过 8 项 / 失败 0 项 / 小结：通过 24 项 / 失败 0 项 / 小结：通过 14 项 / 失败 0 项
  第七组 ①–⑯： 第七组小结：通过 63 项 / 失败 0 项
  第七组 ⑰–㉟： 第七组小结：通过 85 项 / 失败 0 项
  密码库端到端： 密码库小结：通过 7 项 / 失败 0 项

三、本轮两个新功能的实测片段
  第七组 ㉞ 伪终端：App 内命令（不是系统 shell），文件操作关在私有目录里
      -> cwd ~
    PASS pwd 在一个受限根目录里
      -> adsim.js  demo/
    PASS hide 模式：那条广告资源照常加载（没被回空，JS 探测不到被拦）（number）
    PASS 切到「只阻断」后，同一资源被回空（对照组：这次它没加载上）
    PASS 切回「只阻断」后，同一地址被回空（对照组）

四、复现性
     PASS 逐条一致：62 个条目名称/CRC/大小全同 → 源码快照能干净复现这个构件
  == 5/5 收尾 ==
     临时构建产物已清（保留源码副本 /vol1/1000/aicache/buildtmp/cdp-repro 供人工复核）
  复现退出码=0

五、环境事实
  · 模拟器容器本轮仍会偶发 SIGSEGV（Exited 139），所以第七组按段落拆两段跑（断言未删减）
  · 第六组在刚装完包立刻跑时偶发报界面没就位（地址栏 dump 没稳定）：隔一会儿单跑 14/0——这是环境时序，不是应用缺陷
