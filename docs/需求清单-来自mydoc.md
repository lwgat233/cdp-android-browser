1. cookies可以查看登录了哪些网站，网站注册了哪些cookies包括查看httponly的。还有就是可以查看每个cookies的标记和信息（弹出消息小窗口界面）
2. 历史是无限下拉的界面，包含时间，可以导出历史记录。历史是可以从前删除日期的历史也可以从后删除。也可以指定某个域名网站或某些域名网站
3. 有广告拦截功能，拦截规则学网上有的，要自己来和导入规则。还有结合录制功能屏蔽掉一些广告（一般是屏蔽不是阻断，要不然某些网站的js会探测到并不提供服务。是不渲染）。广告拦截里面有黑名单，只要碰到某些网站强制不调整，继续保持该页面
4.有密码存储功能，只有验证了锁屏才能查看和修改。提供随机密码功能。当然要覆盖大写小写特殊字符三个板块，用户可以自定义这三个板块哪些可以有哪些不可以有（应对不同网站密码设置）。密码登录时提示用户是否保存
5. 具有分享功能，如果是视频之类的嗅探资源还可以有个按键是是否发现对应资源
6. 下载栏也是下拉的，可以删除历史，但要询问是否删除对应文件。下载到download的一个目录里面。提供ara2等等下载方式
7. 内置ffmpeg和一个伪终端界面，后续接口以后开发，主要面向便携自动化操作
8. 可以通过ffmpeg强制下载视频，主要还是在资源嗅探栏目
9. 具有网络功能，可以查看最近的发送的包和协议等等的内容（要类似chrome的功能可以筛选文件功能）。可以统计上行和下行以及链接的通路情况。可以查看本地IP对应实际地址和服务器IP对应地址。在网络功能中，还提供丰富的网络测试模块，包括网速，通路，查看实际报中含有内容和二进制（按field查看）。这个可能需要子板块。
10. 还有省电板块，大概可以有两类排序，一类是按照耗电排行，一类是板块。这样点击按键可以取消或者打开功能。
11.提供ai接口，可以让ai搜索查阅内容。不需要交谈。只是给出端口可以将内容交互逻辑交给接口是的其他软件可以通过接口操作网站和搜索。当然（默认）不提供cookies等等隐私数据给ai
12. 还支持config模块，这样可以更好的是的ai能只调用自己的板块达到数据隔离的效果
13 提供多窗口功能
14. 提供一个插件，可以监听每个网页阅读和查看时间
15. 提供后台前台话功能，是的即使熄屏一些操作和行为仍然是在执行
16. 每个窗口都有屏蔽音频播放，等等屏蔽访问一些数据的功能
17. 其他软件打开功能，要主动提示从哪个软件打开，且需要用户主动确认
18. 具有翻译功能，翻译功能，能不能下载Firefox的离线翻译功能呢
19. 提供一个插件可以将网页提取成纯文本。并可以修改如文本颜色，波浪线等等的文本功能。当然里面还是有图片的
20. 主页不再是cdp介绍，介绍隐藏在介绍栏目里，主要是项目介绍和版本等等的
21. 基本上所有都支持一件导出功能
22. 开发一个插件关于伪装与比如豆包ai交互的简洁界面，具备基本的markdown渲染功能。
23. 为cdp的cdp提供大量接口并给予基础接口开发大量基于此的二级接口
24. 有一个一键导出功能，选择弹出的窗口导出哪些文件。也支持导入
25. 探测网站的安全性包括证书之类的，做到一些基础的屏蔽
26. 除了开发的网页，其他网页不能操作软件且访问开发的网页达成访问

27. 有页内查询功能
28. 将屏幕录制这些功能看成一个插件

---

## 逐条状态（本轮结束时）

| # | 需求 | 状态 | 在哪 / 边界 |
|---|---|---|---|
| 1 | Cookie 查看/详情小窗 | ✅ | `/api/cookies*`、控制台 Cookie 栏目（字段拿不到的会如实说明） |
| 2 | 历史无限下拉 + 时间 + 导出 + 按日期/域名删 | ✅ | 历史栏目、`/api/history/*`、导出到系统下载目录 |
| 3 | 广告拦截（规则可加/导入 + 黑名单） | ✅ | `AdBlock.kt`：域名/关键字黑名单 + **"屏蔽不渲染"方式（both/hide/block）**；hide 只注入 CSS 隐藏元素、不阻断请求（避免被 JS 探测） |
| 4 | 密码存储（锁屏验证 + 随机密码 + 三类字符自定义 + 登录提示保存） | ✅ | `PassVault.kt`、`/api/vault*`、控制台「安全」栏目；AES/GCM + Keystore，端到端 7/0 |
| 5 | 分享 + 嗅探资源发现按键 | ✅ | `/api/share`、嗅探条目可点开播放器 |
| 6 | 下载栏下拉 + 删历史问是否删文件 + download 目录 | ✅ | 下载栏目、App 私有 `files/downloads`（不申请存储权限） |
| 7 | 内置 ffmpeg + 伪终端界面 | ✅ | **ffmpeg 已内置**（ffmpeg-kit-min 6.0-2，LGPL，约 27MB/ABI，跑在应用进程内）；**伪终端** `/api/term`（命令见说明，路径关在私有目录） |
| 8 | 用 ffmpeg 强制下载视频 | ✅ | `/api/ffmpeg/remux`：`-c copy` 把 m3u8/HLS 拉成一个 MP4（不重编码）；产物用 ffprobe 复核过时长/流 |
| 9 | 网络：包/协议查看、筛选、上下行统计、通路、IP 归属、报文内容/二进制 | 🟡 部分 | 时间线+统计+解析/证书/通路/测速已做；**报文内容/二进制看不到**——WebView 不给响应体，只能记请求侧（已在界面写明） |
| 10 | 省电板块（按耗电/按板块排序 + 点按键开关） | ✅ | 省电栏目、`/api/power*` |
| 11 | AI 接口（查内容/操作，默认不给隐私） | ✅ | `/api/ai/context`、`/api/ai/act`（实测默认不带表单值/Cookie） |
| 12 | config 模块 / 数据隔离 | ✅ | `space`：历史/书签/脚本/设置各一套，`?space=` 或 `X-CDP-Space` |
| 13 | 多窗口 | ✅ | `/api/win*`；**共用一个 WebView 的逻辑窗口**（省内存，界面写明） |
| 14 | 阅读时间插件 | ✅ | `assets/scripts/readtime.user.js` |
| 15 | 后台前台化（熄屏也跑） | ✅ | `KeepAliveService` |
| 16 | 每个窗口屏蔽音频/数据 | ✅ | 静音跟窗口走；拦截是全局黑名单（不做逐窗口规则，界面写明） |
| 17 | 外部打开要用户确认 | ✅ | 先弹"用哪个 App 打开"，不确认不跳 |
| 18 | 翻译（Firefox 离线翻译那种） | 🟡 部分 | `/api/translate` 通路已做，**离线模型要自己放**（不分发模型）；没配就明说没配 |
| 19 | 纯文本插件（可改文字样式、保留图片） | ✅ | `assets/scripts/plaintext.user.js` |
| 20 | 主页简洁，介绍挪进介绍栏目 | ✅ | `ui/start.html` + 控制台「介绍」 |
| 21 | 一键导出 | ✅ | `/api/export/*` |
| 22 | 豆包风格 AI 简洁界面 + Markdown | ✅ | `assist-panel.user.js`（不内置 AI 服务） |
| 23 | 大量接口 + 二级接口 | ✅ | 140+ 路由；二级接口 `/api/help`、`/api/summary`、`/api/grab`、`/api/batch` |
| 24 | 一键导出/导入（选类别） | ✅ | zip 打包到系统下载目录；导入按地址去重合并 |
| 25 | 网站安全（证书 + 基础屏蔽） | ✅ | `/api/security/page` + 屏蔽名单警告条（不阻断） |
| 26 | 别的网页不能操作软件 | ✅ | 页面事件/元信息无口令一律 403（每次启动随机口令，藏在注入闭包局部变量里） |
| 27 | 页内查询 | ✅ | 高亮命中 + 上一个/下一个 + 清除 |
| 28 | 屏幕录制（做成插件） | ✅ | `ScreenRec.kt`、`/api/screen*`（**要用户亲手在系统弹窗点"开始录制"**） |

**还差的**：第 9 条的报文内容/二进制（WebView 不给响应体，只能记请求侧）、第 18 条的离线翻译模型文件（要自己放模型）。

**ffmpeg 的边界**（写在接口返回里）：用的是 `ffmpeg-kit-min`（LGPL 变体，**不含 x264/x265 等 GPL 编码器**），所以能做读取信息与转封装（`-c copy`），**不能重编码**；要重编码得换 gpl 变体（体积和许可都会变）。

